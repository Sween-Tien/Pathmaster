#ifndef ARRAYPR0CESSOR_H
#define ARRAYPROCESSOR_H
#include <vector>
#include <QMessageBox>

class Greedyarray {
public:
    static void arrayGreedy(const std::vector<std::vector<int>>& array, QWidget* parent);
};

#endif // ARRAYPROCESSOR_H#pragma once
